# MODULE · V-M10 校准钩子（CAL）

> 所属：`modules/V-M10-校准钩子/`｜层：L0｜版本：v1.0（2026-09-21）｜引擎：`VESI-ENGINE.md` §1

## 目的 / Purpose
**L0 仅声明**：存在校准协议文件、生成侧只读挂接；个人校准数据属 L2，不入模块门禁。

## 输入 → 输出 / IO
- 输入：—（只读挂接）
- 输出：无产物；提供边界声明与指针

## 接口字段 / Interface
L0：`references/outcome-anchor-protocol.md`（存在性）；L2：D₀ 细则/执行锚/处置——本机节点，不进模块硬依赖。

## 装载 / Loading（指针，细节在正本）
`references/outcome-anchor-protocol.md`（只读）

## 门禁 / Gates
只声明「存在校准协议文件、生成侧只读挂接」；**不得**把某次锚分/命中率/冻结台账分数写成模块通过必要条件

## 降级 / Fallback
不挂接亦可走全链（校准为可选增强）

## 样例 / Example（合成或去敏）
—（无样例；协议文件自身即说明）
