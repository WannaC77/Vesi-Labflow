## 变更说明 / What changed

<!-- 一句话说清「改了什么、为什么」。涉及公开口径/断言/门禁时，务必写明依据。 -->

## 自测命令 / How it was verified

```bash
# 至少贴一条真实跑过的命令与结果（不要写「应该没问题」）
```

- [ ] `python tools/env_check.py`（或 `tools/env_check.py --selftest`）通过
- [ ] 相关 `--selftest` 通过（改了哪个件贴哪个）
- [ ] 若改了文档里的**可复验断言**（件数 / 页数 / 覆盖范围）：已按当前磁盘实测重写

## 合规自查 / Compliance

- [ ] 未引入第三方受限素材（新素材已登记 `THIRD-PARTY.md`）
- [ ] 未在包内写入本机绝对路径 / 私有目录名（`paths` 与脱敏扫描在包内应保持零命中）
- [ ] 文档与实现一致（退出码 / 覆盖范围 / 随包与外置件）
