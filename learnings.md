# learnings — 错题本（空白模板）

> **用途 / Purpose**：正式交付后记一条经验，下次不犯重复错误；本文件同时是**自改进闭环的入口件**。
> **分发状态**：本文件随包分发时**不含任何条目**——第一条由你自己写；下面的格式与机制即开箱模板。
> **会话启动**：读本文件**最后 20 行**（新条目追加在文件末尾）。
> **口径正本**：`workflows/11-知识管理.md`（learnings 提炼闭环）；本文件只放记录格式与提炼机制模板。
> **提炼纪律**：被提炼条目**原位保留、只加标记不删除**（它们是教训精华的证据链）。

---

## 1 · 条目格式（字段说明）

| 字段 | 写法 | 说明 |
|---|---|---|
| 日期 | `[YYYY-MM-DD]` | 记录当天日期 |
| 工作流编号 | `[工作流编号]` | `01`–`13` / `_SHARED` / 模块号（`V-M*`）；跨多工作流写「全局·<主题>」 |
| 做得好 | `做得好: …` | 值得复用的做法（下次照做） |
| 需改进 | `需改进: …` | 现象 + 根因；根因判断必须带三态标注 |
| 改进措施 | `改进措施: …` | 落到**具体文件 / 清单条目 / 门禁**的动作，不写口号 |
| 三态标注 | `[事实]` / `[推断]` / `[假设]` | 逐条标注，未标注的断言不进条目 |
| 复发计数 | `复发 N 次` | 同主题每出现一次 +1；首见写「首见」 |
| 状态标记 | `[个案保留 · 未达提炼门槛]` / `[已提炼 YYYY-MM → <落点>]` | 提炼后**原位保留并打标**（不删除、不压缩），作为经验教训条目的证据链 |

**记录纪律**：每次上限 3 条，宁缺毋滥；只在「正式交付物完成」或「踩了值得记的坑」时记录。

---

## 2 · 条目模板（复制使用）

```text
[YYYY-MM-DD][工作流编号] 做得好: <值得复用的做法> / 需改进: <现象；根因标 [事实]/[推断]/[假设]> / 改进措施: <落到哪个文件、清单或门禁>
  > `[复发 N 次]` `[状态标记]`
```

---

## 3 · 提炼机制（候选 → 复核 → 落盘）

### 3.1 触发：阈值 + 主动窗口

| 触发方式 | 口径 | 备注 |
|---|---|---|
| 阈值触发 | 有效记录 **≥30 条** → 触发一轮提炼 | **经验值**，可按团队节奏调整 |
| 主动窗口①（不等阈值） | 新增记录跨**两条及以上链路** | 如记录类与交付类同批出现 |
| 主动窗口②（不等阈值） | 单条记录已定位到**机制级根因**（非个案） | 个案不入，根因才入 |
| 主动窗口③（不等阈值） | 累计新增 **≥8 条**，且旧条目自上次提炼后无变更 | **经验值** |
| 单轮条数上限 | **≤6 条** | **经验值**，宁缺毋滥 |

> 阈值与主动窗口同时满足时按主动窗口执行；主动窗口提炼后，该批记录**不计入**下一次阈值计数（避免同一批记录被重复提炼）。

### 3.2 入选门槛

- **同主题复发 ≥2 次才入选**（**经验值**）：仅出现 1 次的高价值个案**留在本文件原位**，不提炼。
- **归属判定**：经验教训落在**执行该规则**的工作流文件中——通用纪律与自查类 → `workflows/_SHARED.md`；记录与排障类 → `workflows/12-实验执行与排障.md`；统计与图件类 → `workflows/08-数据处理与可视化.md`；知识与闭环类 → `workflows/11-知识管理.md`；领域口径类 → 对应领域工作流（`workflows/03`–`07`）。
- **落点位置**：写在工作流文件的「Checklist」标题上方，小节名 `## 经验教训（已提炼）`，条目编号形如 `LS-N`，并标 `[已提炼 YYYY-MM]`。

### 3.3 三步执行

| 步 | 做什么 | 产物 / 判据 |
|---|---|---|
| ① **提炼候选** | 按主题归并记录、统计复发次数、判定归属落点 | 提炼候选报告：主题 / 复发次数 / 拟落点 / 原文条目引用 |
| ② **复核** | 逐条核五项：是否写入 · 表述是否需改 · 是否并入既有经验教训条目 · 归属落点是否正确 · 复发计数是否准确 | 复核意见（通过 / 退回补充） |
| ③ **落盘** | 写入目标工作流文件的「经验教训（已提炼）」小节；本文件对应条目**原位保留**并打 `[已提炼 → <落点>]` | 改后文件 + 标记行 |

### 3.4 写入门禁

- **自动化流程**（定时任务 / 批处理 / agent 自跑）**不得直接写入**本文件与工作流正文；只允许产出「提炼候选报告」，由人复核后落盘。
- 提炼后**本文件不清零**：被提炼条目原位保留并打标，保证教训可回溯到原始表述。
- 正本文件改动须带同批备份与回读校验；写错会污染后续所有轮次的判据来源。

---

## 4 · 空白记录区

<!-- 记录从这里开始 -->

---

**变更记录**

- v1.0（公开版）：空白模板建立——条目字段 / 三态标注 / 复发计数 / 状态标记 + 提炼机制（触发 → 入选门槛 → 候选·复核·落盘 → 写入门禁）。既有条目已整体留空，只保留格式与机制。
