# Vesi-Labflow · BOOT（冷启动件 · ≤2 KB）

> **一句话**：面向**药学与生命科学**的科研工作流引擎——把「文献 → 实验设计 → 数据记录 → 统计分析 → 论文装配」拆成 **11 模块 + 1 底座**，每模块带**可执行门禁**与**显式降级路径**。
> **本产品不依赖**任何特定 agent、CLI、调度器或 API key：任何能读文件 + 跑 Python 3.11+ 的人或 agent 都可用。

## 五条铁律（摘要）

1. **诚实降级**：缺件（OCR / Word / LaTeX / 字体 / 依赖）标 `[降级]` 并留证，**禁止**假装成功。
2. **三态标注**：结论标 事实 / 推断 / 假设，不混用。
3. **Claim-Evidence**：论文每处主张可回溯到证据件（图 / 表 / 记录 / 代码）。
4. **统计纪律**：n 与统计方法**实验前定稿**；n=3 只是下限不是目标；多重比较必须校正。
5. **学术诚信**：AI 只做辅助，实验数据与结论由你独立产出（见 `workflows/` 与 `references/` 相关条款）。

## 三步装载

1. 读本文件 → 读 `VESI-CORE.md` → 读 `VESI-ENGINE.md`
2. 按任务选 runbook，加载对应 `workflows/NN-*.md` + `workflows/_SHARED.md`（`NN` 为两位序号）
3. 需要计算 / 门禁时调用 `tools/` 或 `scripts/`；缺件按模块表「降级」列执行并标 `[降级]`

## 指针

| 用途 | 文件 |
|---|---|
| 5 分钟上手 | `QUICKSTART.md` |
| 完整说明 | `README.md`（英文 `README.en.md`） |
| code agent 装载 | `AGENTS.md` |
| 模块表 / 门禁 | `VESI-ENGINE.md` · `VESI-CORE.md` |
| 数据与统计工具 | `tools/`（NCA · 房室拟合 · 释放拟合 · 统计管线 · 冒烟链） |
| 交付门禁脚本 | `scripts/` |
| 私有校准（L2 · 可选自建） | `kb/` · `<共享层>/` · `<校准台账>` · `<锚注册卡>/` |
