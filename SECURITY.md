# 安全策略 / Security Policy

本仓库是**离线优先**的工作流与工具集：无服务端、无数据库、不内置任何密钥。
安全问题主要落在两类：**依赖与脚本执行**、**包内是否混入敏感信息**。

## 支持的版本

| 版本 | 支持状态 |
|---|---|
| v1.0.x（当前公开发行） | ✅ 接收报告 |
| 更早的内部快照 / 私有分支 | ❌ 不接收（请勿使用） |

## 报告方式（请勿用公开 issue 报告安全问题）

1. 首选：在仓库的 **Security → Report a vulnerability**（GitHub 私有漏洞报告通道）提交，内容仅维护者可见。
2. 若该通道不可用：请开一个**不含细节**的公开 issue，说明「需要一个私密联系方式」，维护者会回复私密通道。
3. 请勿在公开 issue / PR / 讨论中粘贴可利用细节、真实数据或密钥。

## 我们希望报告包含

- 受影响版本或提交号、文件路径；
- 复现步骤（命令 + 输入样例，**用合成数据**，不要真实实验数据）；
- 实际结果与期望结果；
- 影响评估（例如：任意代码执行 / 路径穿越 / 依赖供应链 / 敏感信息混入发布包）。

## 处理流程与时限（尽力而为）

| 阶段 | 目标时限 |
|---|---|
| 确认收到 | 7 天内 |
| 初判与分级 | 14 天内 |
| 修复或给出缓解方案 | 视复杂度，通常 30 天内 |
| 公开披露 | 修复发布后，经报告者同意 |

## 范围说明

**在范围内**：`tools/`、`scripts/`、`.github/workflows/` 中的代码缺陷（含依赖声明与执行链）；
发布包中混入的绝对路径、机构/个人信息、密钥或其他敏感内容。

**不在范围内**：第三方件自身的漏洞（请报给上游，见 `THIRD-PARTY.md`）；用户自行改写的私有数据与配置；
本包**不随附**的第三方件（外部模板、数据集、字体等）引发的许可与合规问题（属许可证议题，见 `THIRD-PARTY.md`）。

## 安全使用建议

- 在虚拟环境中安装依赖（`<venv>/`），不要把本包装进系统解释器。
- 自检 / 冒烟脚本会产生中间产物（`_smoke_out/` 等）；**不要在只读或含真实数据的工作目录里直接跑**，请在副本中执行。
- 本包不会主动联网；涉及文献检索的步骤需你自行提供检索渠道与凭据，凭据请用环境变量管理，勿写入仓库。
