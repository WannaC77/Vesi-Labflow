# 支持 / Support · Vesi-Labflow

## 这是什么

Vesi-Labflow 是一套「手册 + 工具链」形态的药学与生命科学科研工作流（详见 [README.md](README.md)）。**这里只回答"用的过程中出了什么问题"**——方法论与口径问题请直接读 `workflows/` 与 `references/` 对应正本，不在支持范围。

## 遇到问题怎么办

1. **先跑环境自检**（能定位大半问题）：

   ```bash
   python tools/env_check.py --selftest     # 环境档位 T0/T1/T2 + 缺件清单
   python tools/smoke_chain.py --selftest   # 全链冒烟（含黄金数值断言）
   ```

   退出码语义：`0` 通过 · `1` 失败 · `2` 用法错误 · `3` 依赖缺失未执行。缺件照实标 `[降级]`，缺件不判 FAIL——自检输出里的 `[降级]` 行会告诉你缺什么、可不可以继续。

2. **搜现有 issue**：先搜 [Issues](../../issues)，避免重复开帖。

3. **没有现成答案 → 开 issue**：
   - 缺陷/报错：用 [bug_report 模板](../../.github/ISSUE_TEMPLATE/bug_report.md)，**必须带**：复现命令 + 实际输出 + 期望输出（统计/PK 结论类问题请附你自检的输出与数据形态说明，**不要贴未脱敏的真实实验数据**）。
   - 用法/建议：用 [feature_request 模板](../../.github/ISSUE_TEMPLATE/feature_request.md)。
   - **药学科研场景提醒**：多数"跑不通"是缺依赖（`rc=3`）或表头不符合 `references/数据条目字典.md` 的字段约定——自检与模板 README 会直接列出来，先对一遍再发帖。

## 回应时间承诺

| 事项 | 承诺 |
|---|---|
| 首次回应（标签分诊） | ≤ 7 天 |
| 缺陷 triage 结论（确认 / 复现 / 转文档） | ≤ 14 天 |
| 影响安装自检的紧急缺陷 | 尽力优先，但不提供任何"响应时间保证"——本仓库由维护者业余时间维护 |

超过承诺时限无人回应？在原 issue 下礼貌地跟帖催一次即可。

## 安全问题（不要开公开 issue）

漏洞、依赖投毒嫌疑、敏感数据处理风险 → 走 [SECURITY.md](SECURITY.md) 的私下报告渠道，**不要**在公开 issue 里贴细节。

## 明确不支持的

- 论文代写、数据代分析、"一键出结果"（本包明确不做这些，见 README「不服务」）；
- 与本仓库无关的通用统计咨询（去统计方法社区或请教你的统计老师）；
- 私有校准数据的接入（`V-M10` 只声明边界，不在开源支持范围）。
